{{- if .Values.dashboard.enabled }}
{{- $ns := include "nantian-gw.namespace" . }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "nantian-gw.name" . }}-dashboard
  namespace: {{ $ns }}
  labels:
    {{- include "nantian-gw.dashboard.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "nantian-gw.dashboard.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 3000
      targetPort: http
{{- end }}